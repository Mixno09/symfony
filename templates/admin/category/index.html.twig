{% extends 'admin/layout.html.twig' %}

{% block title %}Категории{% endblock %}

{% block h1 %}Категории{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <a href="{{ path('product_create') }}" class="btn btn-success mb-2">
                <span data-feather="plus"></span> Создать
            </a>
        </div>
        <div class="col-md-12">
            <table class="table table-bordered table-hover">
                <thead>
                <tr class="d-flex">
                    <th scope="col" class="col-md-2">ID</th>
                    <th scope="col" class="col-md-6">Название</th>
                    <th scope="col" class="col-md-4">Действия</th>
                </tr>
                </thead>
                <tbody>
                {% for category in pagination %}
                    {# @var \App\Entity\Category category #}
                    <tr class="d-flex">
                        <th scope="row" class="col-md-2">{{ category.id }}</th>
                        <td class="col-md-6">{{ category.title|u.truncate(64, '...') }}</td>
                        <td class="col-md-4">
                            <a href="{{ path('product', {'slug': category.slug}) }}" target="_blank"
                               class="btn btn-outline-primary text-nowrap">
                                <span data-feather="eye"></span> Просмотреть
                            </a>
                            <a href="{{ path('product_update', {'id': category.id}) }}"
                               class="btn btn-primary text-nowrap">
                                <span data-feather="edit"></span> Редактировать
                            </a>
                            <a href="{{ path('product_delete', {'id': category.id}) }}"
                               class="btn btn-danger text-nowrap">
                                <span data-feather="x-square"></span> Удалить
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-md-auto">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}